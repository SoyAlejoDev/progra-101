# App-specific ignores (inherited from root .gitignore)
# This file is kept for compatibility but most ignores are in root .gitignore

# App-specific build outputs
dist-ssr/

# App-specific environment files
*.local
